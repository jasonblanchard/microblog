<%= form_for([@post, @post.comments.build]) do |f| %>
    <div>
        <%= f.label :body %>
    </div>
    <div>
        <%= f.text_area :body, :rows => 4 %>
    </div>
    <%= f.submit %>
<% end %>
